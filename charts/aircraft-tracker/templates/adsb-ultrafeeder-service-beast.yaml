apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.beast.host }}
  labels:
    app: adsb-ultrafeeder
spec:
  type: ClusterIP
  selector:
    app: adsb-ultrafeeder
  ports:
    - protocol: TCP
      port: {{ .Values.beast.port }}
      targetPort: {{ .Values.beast.port }}